
* Installation:
Install atos-utils tools with:
$ make
$ make check
$ sudo make install

Default installation prefix is /usr/local.

Change installation prefix with for instance:
$ make PREFIX=/home/myself/local install

* Requirements:
atos-utils requires the following packages to be installed on your system:
* PRoot with addons support: 
  Obtain it with: git clone user@hostname/gitroot/qemu/proot-dev-guillon.git
  Build it with:
  $ make -C src ENABLE_ADDONS="cc_deps cc_opts"
  $ sudo make -C src install
* Python pygraph modules:
  Obtain them from: http://code.google.com/p/python-graph/
  Dowload and install:
  * python-graph-core-1.8.1.tar.gz
  * python-graph-dot-1.8.1.tar.gz
